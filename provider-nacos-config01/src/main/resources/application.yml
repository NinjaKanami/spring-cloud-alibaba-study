server:
    port: 8080

spring:
    application:
        name: service-provider-local
    cloud:
        nacos:
            config:
                username: nacos
                password: nacos
                server-addr: 127.0.0.1:8848
                file-extension: yml


    config:
        import: optional:nacos:provider-config-remote
